specVersion: "v1"
serviceId: "terminales"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "terminales-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
